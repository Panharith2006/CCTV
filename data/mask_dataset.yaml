## Base dataset directory (relative to repo root). Change to absolute path if your
## dataset is stored elsewhere, e.g. `D:/mydata/mask_dataset`.

# Use an absolute path so Ultralytics doesn't prepend its download directory.
# Update this if your repository is in a different location.
path: D:/AHBD/CCTV_AI/dataset/mask_dataset

# `train` and `val` are paths relative to the `path` value above
train: images/train
val: images/val

names:
  0: person
  1: mask
  2: helmet
