# Dataset configuration for mask and helmet detection
# Update the 'path' if your dataset is stored elsewhere

# Absolute path to dataset directory
path: D:/AHBD/CCTV_AI/data/mask_dataset

# Paths to train/val/test sets (relative to 'path')
train: train/images
val: val/images
test: test/images

# Number of classes
nc: 2

# Class names (0-indexed)
names:
  0: mask
  1: helmet
